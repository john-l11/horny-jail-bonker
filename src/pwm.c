#include "../headers/includes.h"

void pwmInit() {}

/**
 * @brief
 * This function will be used once a dm is detected by mr horny himself.
 * it will alter the width of the pulse to make sure that the motor moves
 * the fly swatter to smack john on the head and then it will close the circuit
 * of the swatter to trigger the shock.
 */
void bonk() {}